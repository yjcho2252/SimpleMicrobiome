# Release 2026-05-04

## Summary
- Consolidated package loading in `app.R` and removed redundant per-module `library(...)` declarations.
- Refined multiple UI labels and defaults for clearer behavior and wording consistency.
- Updated clustering result text in Beta Diversity to explicitly name the clustering method.
- Added a new black-and-white app icon asset.

## Changed
- App bootstrap (`app.R`):
  - Added shared package imports: `httr2`, `readr`, `dplyr`.
- Module dependency cleanup:
  - Removed redundant top-level `library(...)` calls from:
    - `modules/mod_ancom.R`
    - `modules/mod_preprocessing.R`
    - `modules/mod_taxa_comparison.R`
    - `modules/mod_convert.R`
- UI and wording updates:
  - `modules/mod_alpha.R`: changed default plot height from `400` to `500`.
  - `modules/mod_fileload.R`: changed label text from `Metadata File` to `Metadata Table`.
  - `modules/mod_beta.R`: added clustering status line `Method: PAM (Partitioning Around Medoids)`.
  - `README.md`: updated feature wording for Taxa Comparison and Association Biplot (`dbRDA/CCA`).
  - `modules/mod_top.R`: added update bullet `260502: CCA function in Association Biplot added.`
- Code organization comments:
  - Added `## UI` / `## Server` section comments in:
    - `modules/mod_biplot.R`
    - `modules/mod_heatmap.R`
    - `modules/mod_convert.R`
- Assets:
  - Added icon file `www/icon_bw2.png`.

## Notes
- This release is focused on dependency cleanup, wording clarity, and UI consistency without changing core analysis workflows.
